using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectSpawner : MonoBehaviour
{


    /*
    IEnumerator CactusSpawner()
    {
        while (true)
        {
            yield return new WaitForSeconds(cactusSpawnerInterval);
            
           
                tmp = ObjectPool.instance.GetRandomCactus();
                if (tmp)
                {
                    tmp.transform.position = spawnPoint.transform.position;
                    tmp.SetActive(true);
                }
                cactusSpawnerInterval = Random.Range(2, 4);
            }
        }
    */
}
